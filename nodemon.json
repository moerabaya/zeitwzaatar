{
  "watch": ["server"],
  "ext": "ts, graphql, gql",
  "exec": "ts-node ./server/index.ts && eslint server --ext .ts",
  "ignore": ["server/**/*.spec.ts", "node_modules"],
  "env": {
    "NODE_ENV": "development"
  }
}
